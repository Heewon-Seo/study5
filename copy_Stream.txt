importjava.io.ByteArrayInputStream;importjava.io.ByteArrayOutputStream;importjava.util.Arrays;/*I/O(text파일을기반으로함)stream-입출력의중간통로(매개체):데이터통로.default크기가있다(1byte)JAVAAPI기본적인설계가되어있다[추상클래스]로...InputStream,OutputStream사용하고싶다면,입출력대상에따라서추상클래스를재정의하여사용하세요1.Memory:(기존에사용했던Array,Collection)ByteArrayInputStream,ByteArrayOutputStream2.*File*:FileInputStream,FileOutputStream주의:한빨대에는byte만통과할수있다...(한글같은거넣으면빨대찢어짐)*/publicclassEx01_Stream{publicstaticvoidmain(String[]args){//byte(-128~127)정수를저장하는데이터타입byte[]inSrc={0,1,2,3,4,5,6,7,8};//초기화byte[]outSrc=null;//현재메모리를가지고있지않음(초기화되어있지x)//******입출력의[대상]이있다면중간연결통로를만들어서작업******//(빨대만들깅..왜두개만드냐면하나의빨대가두개역할을못하니까)ByteArrayInputStreaminput=null;ByteArrayOutputStreamoutput=null;input=newByteArrayInputStream(inSrc);//inSrc배열(메모리)>>read할준비output=newByteArrayOutputStream();////확인System.out.println("outSrcbefore:"+Arrays.toString(outSrc));//null//공식같은로직...정형화된것/*intdata=0;//로컬배리어블만듦while((input.read())!=-1){//read는값을다음으로넘겨버려서...이케한다System.out.println(input.read());//1357-1}*/intdata=0;//로컬배리어블만듦while((data=input.read())!=-1){//input.read빨아들임.//배열에값이없으면-1이뜬다...즉,-1이아니다라는건'배열에값이있을때'//System.out.println(data);이러면출력까지함output.write(data);//write의대상이output객체//ByteArrayOutputStream이자신의통로에data를가지고있음}System.out.println(output.toByteArray());//생성된메모리의주소[B@156643d4outSrc=output.toByteArray();//자기가가진주소를할당System.out.println("outSrcbefore:"+Arrays.toString(outSrc));//outSrcbefore:[0,1,2,3,4,5,6,7,8]}}